name: climpred-minimum-tests
channels:
  - conda-forge
  - defaults
dependencies:
  - xesmf
  - esmpy
  - dask
  - netcdf4
  - ipython
  - nc-time-axis
  - coveralls
  - pytest
  - pytest-cov
  - pip
  - pip:
    - pytest-lazy-fixture
    - -e ../..
