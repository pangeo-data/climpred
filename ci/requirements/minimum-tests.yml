name: climpred-minimum-tests
channels:
  - conda-forge
  - nodefaults
dependencies:
  - python>=3.7
  - cftime>=1.1.2
  - coveralls
  - dask-core
  - ipython
  - netcdf4
  - pip
  - pytest
  - pytest-cov
  - pytest-xdist
  - scipy
  - xarray>=0.19.0
  - xskillscore>=0.0.18
  - pip:
    - pytest-lazy-fixture
    - -e ../..
