name: climpred-maximum-tests
channels:
  - conda-forge
  - nodefaults
dependencies:
  - python>=3.7
  - cftime>=1.1.2
  - coveralls
  - dask-core
  - eofs
  - esmpy=*=mpi*  # Ensures MPI works with version of esmpy.
  - ipython
  - matplotlib-base
  - nc-time-axis>=1.4.0
  - netcdf4
  - pip
  - pytest
  - pytest-cov
  - pytest-xdist
  - scipy
  - xarray>=0.19.0
  - xesmf
  - xrft
  - xskillscore>=0.0.18
  - xclim
  - bias_correction
  - pip:
    - pytest-lazy-fixture
    - -e ../..
